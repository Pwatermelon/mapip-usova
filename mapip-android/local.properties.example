# Скопируйте в `local.properties` (рядом с этим файлом) и подставьте значения.
# Файл `local.properties` в репозиторий не коммитится.

sdk.dir=/path/to/Android/sdk
MAPS_API_KEY=your_google_maps_key_for_maps_sdk
